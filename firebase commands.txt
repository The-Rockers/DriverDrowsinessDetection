save emulator data on exit
firebase emulators:start --export-on-exit=./saved-data

import data from
firebase emulators:start --import=./saved-data

combined command (USE THIS ONE!!!)
firebase emulators:start --import=./saved-data --export-on-exit

firebase deploy --only functions